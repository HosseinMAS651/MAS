(()=>{'use strict';const d=JSON.parse(document.getElementById('recordings-data')?.textContent||'{}');document.querySelectorAll('.delete-file').forEach(b=>b.addEventListener('click',async()=>{if(!confirm('این فایل حذف شود؟'))return;b.disabled=true;try{const fd=new FormData();fd.append('csrf',d.csrf||'');const r=await fetch(`/files/${b.dataset.fileId}/delete`,{method:'POST',body:fd,credentials:'same-origin',headers:{Accept:'application/json'}});if(r.status===401){location.href='/login';return}if(!r.ok)throw new Error();location.reload()}catch(_){b.disabled=false;alert('حذف فایل ناموفق بود.')}}));})();
